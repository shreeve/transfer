import SwiftUI
import TransferUI

@main
struct TransferApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .defaultSize(width: 720, height: 480)
    }
}
